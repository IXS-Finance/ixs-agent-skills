# Execution Notes

This skill should distinguish planning from execution clearly:

- planning mode prints the quote and intent only
- execution mode follows the returned transaction steps in order

Returned calldata and transaction parameters must come from the intent response without manual reconstruction.
